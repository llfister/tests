{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Variant edit</h1>

    <div class="well">
    {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'}}) }}
    {{ form_errors(edit_form) }}
    	<div class="form-group">
    		{{ form_label(edit_form.categoryId, 'Category Id', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.categoryId) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.categoryId, {'attr': {'class': 'form-control'}}) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.productId, 'Product Id', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.productId) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.productId, {'attr': {'class': 'form-control'}}) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.createdAt, 'Created At', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.createdAt) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.createdAt) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.updatedAt, 'Updated At', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.updatedAt) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.updatedAt) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.showFrom, 'Show From', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.showFrom) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.showFrom) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.showTo, 'Show To', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.showTo) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.showTo) }}
    		</div>
    	</div>

    	<div class="form-group">
    		{{ form_label(edit_form.specialPrice, 'Special Price', {'label_attr': {'class': 'col-sm-3 control-label'}} )}}

    		{{ form_errors(edit_form.specialPrice) }}
    		<div class="col-sm-4">
    			{{ form_widget(edit_form.specialPrice, {'attr': {'class': 'form-control'}}) }}
    		</div>
    	</div>

	    {{ form_widget(edit_form.submit, {'attr': {'class': 'btn btn-primary'}}) }}

	    {{ form_rest(edit_form) }}
	    {{ form_end(edit_form) }}
	    
        <ul class="list-unstyled">
		    <li>
		        <a href="{{ path('variant') }}" class="btn btn-primary">
		            Back to the list
		        </a>
		    </li>
		</ul>
	</div>
{% endblock %}
